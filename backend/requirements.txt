# ===========================================================================
#  AIMIP backend — Phase 1 (foundation) runtime + dev dependencies
#  ML / LLM / vector libraries (torch, openai, faiss, chromadb, celery, redis,
#  sentence-transformers) are intentionally NOT installed in Phase 1. Their
#  provider adapters lazy-import those packages and are only exercised in later
#  phases; the abstraction layer, factories and mock/in-memory/in-process
#  adapters run without them. See docs/16_AI_Providers.md and docs/00_Project_Roadmap.md.
# ===========================================================================

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Configuration & validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Database (async MongoDB) ---
motor==3.6.0
pymongo==4.9.2

# --- Security / auth ---
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1

# --- Logging ---
structlog==24.4.0

# --- Production hardening (Phase 6: security, observability, workers) ---
slowapi==0.1.9
prometheus-client==0.21.1
celery==5.4.0
redis==5.2.1

# --- AI / ML (Phase 2: prediction pipeline, Grad-CAM). CPU wheels. ---
torch==2.5.1
torchvision==0.20.1
numpy==2.1.3
pillow==11.0.0
opencv-python-headless==4.10.0.84

# --- RAG (Phase 4: ingestion, embeddings, vector search, retrieval) ---
pymupdf==1.24.14
sentence-transformers==3.3.1
faiss-cpu==1.9.0.post1
rank-bm25==0.2.2
chromadb==0.5.23

# --- Training (Phase 5: dataset, transfer learning, evaluation, artifacts) ---
scikit-learn==1.6.0
matplotlib==3.10.0
pyyaml==6.0.2

# --- HTTP client (health checks, tests) ---
httpx==0.28.1

# --- Dev / quality / tests ---
pytest==8.3.4
pytest-asyncio==0.25.2
asgi-lifespan==2.1.0
mongomock-motor==0.0.35
ruff==0.9.2
mypy==1.14.1
