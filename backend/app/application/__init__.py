"""Application layer: use-case services orchestrating domain ports.

Services contain business logic and depend only on ports (interfaces), never on
concrete adapters or the web framework (see ``docs/07_Backend_Architecture.md``).
"""
